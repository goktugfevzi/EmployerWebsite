import internal from "stream";

export interface IUser {
  userName: string;
  responseData:any;
  email: string;
  departmentId: number;
  emailConfirmed: string;
  id: string;
  departmentIdConverted: string;
}
